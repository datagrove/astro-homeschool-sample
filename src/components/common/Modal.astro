---
const {id, open} = Astro.props;
---
<button 
    onclick={`window.${id}.showModal()`}
    class="border rounded-full p-4 bg-dgSecondary text-white"
>
    { open }
</button>



<dialog id='{ id }'>
  <slot name="main" />
  <form method="dialog">
    <slot name="close" />
  </form>
</dialog>