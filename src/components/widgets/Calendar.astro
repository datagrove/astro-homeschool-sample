---
import { getCollection } from 'astro:content';

const events = await getCollection('events', ({ data }) => {
    return data.draft !==true;
})
---
<div>
    <div class="">
        { 
            events.map(( event ) => (
            <div class="flex items-center m-2">
                <div class="rounded-full flex justify-center items-center bg-dgSecondary w-28 h-24">
                    <p class="text-center text-white" set:html={ event.data.date } />
                </div>

                <div class="ml-4">
                    <p class="font-bold" set:html={ event?.data.title } />
                    <p class="italic" set:html={ event.data.location } />
                </div>
            </div>    
            
            ))
        }
    </div>
</div>